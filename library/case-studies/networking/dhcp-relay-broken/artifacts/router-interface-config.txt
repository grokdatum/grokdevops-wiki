### VLAN 50 Interface (BROKEN -- missing ip helper-address) ###

Router# show running-config interface Vlan50
Building configuration...

interface Vlan50
 description New-Development-VLAN
 ip address 10.50.0.1 255.255.255.0
 no shutdown

### VLAN 20 Interface (WORKING -- has ip helper-address) ###

Router# show running-config interface Vlan20
Building configuration...

interface Vlan20
 description Engineering-VLAN
 ip address 10.20.0.1 255.255.255.0
 ip helper-address 10.10.10.5
 no shutdown

### VLAN 30 Interface (WORKING -- has ip helper-address) ###

Router# show running-config interface Vlan30
Building configuration...

interface Vlan30
 description QA-VLAN
 ip address 10.30.0.1 255.255.255.0
 ip helper-address 10.10.10.5
 no shutdown

### DHCP Server IP ###
# 10.10.10.5 is on VLAN 10 (Server VLAN)

### Router routing table confirms VLAN 50 is routable ###

Router# show ip route 10.50.0.0
Routing entry for 10.50.0.0/24
  Known via "connected", distance 0, metric 0
  Directly connected, Vlan50
