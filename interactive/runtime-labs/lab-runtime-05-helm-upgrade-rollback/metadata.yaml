id: lab-runtime-05
difficulty: 2
time_estimate_minutes: 15
concepts:
  - helm_upgrade
  - helm_rollback
  - helm_values
failure_patterns:
  - FP-011_helm_upgrade_failure
key_commands:
  - helm history <release> -n <ns>
  - helm rollback <release> <rev> -n <ns>
  - helm status <release> -n <ns>
prerequisites:
  - make deploy-all
related_runbooks:
  - training/library/runbooks/cicd/helm_upgrade_failed.md
