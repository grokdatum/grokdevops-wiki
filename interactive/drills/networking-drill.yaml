# Networking Drill — TCP/IP, DNS, TLS, load balancing, firewalls
# See training/interactive/TRAINING_METADATA.md for manifest conventions.

name: Networking Drill
description: 25-minute drill covering TCP/IP, DNS, TLS, load balancing, and firewalls
duration_minutes: 25
sources:
  - type: flashcard
    count: 12
    strategy: weak_first
  - type: quiz
    count: 8
    strategy: balanced
  - type: scenario
    count: 1
    strategy: random
filters:
  levels: [L1, L2]
  topics:
    - tcp_ip
    - dns
    - tls
    - load_balancing
    - firewalls
    - http
    - networking
    - vpn
