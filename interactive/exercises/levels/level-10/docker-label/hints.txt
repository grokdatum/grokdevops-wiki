## Objective
Dockerfile instructions must be in the correct order - install dependencies before copying app

## Hint 1
Think about Docker layer caching. Which files change more frequently?

## Hint 2
Copy package.json first, install deps, then copy the rest of the source.

## Hint 3
Split into: COPY package.json . then RUN npm install then COPY . .

