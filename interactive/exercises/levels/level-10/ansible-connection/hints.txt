## Objective
Fix the connection type so the playbook runs commands on the local machine

## Hint 1
The playbook targets localhost but is trying to connect over the network.

## Hint 2
When running on localhost, you don't need SSH - there's a simpler connection type.

## Hint 3
Change 'connection: ssh' to 'connection: local'.

