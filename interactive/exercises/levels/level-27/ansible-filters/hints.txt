## Objective
Fix the playbook to use the correct Jinja2 filter for YAML output instead of JSON

## Hint 1
The output file should be YAML but is being written in a different format.

## Hint 2
Check the Jinja2 filter being used - to_json produces JSON, not YAML.

## Hint 3
Change 'to_json' to 'to_yaml' to produce valid YAML output.

