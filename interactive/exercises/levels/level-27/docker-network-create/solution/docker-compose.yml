services:
  web:
    image: nginx:alpine
    networks:
      - mynet
  api:
    image: python:3.11-slim
    command: python3 -m http.server 5000
    networks:
      - mynet

networks:
  mynet:
    driver: bridge
