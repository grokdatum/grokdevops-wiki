## Objective
Custom network must use the bridge driver for single-host container communication

## Hint 1
Docker has different network drivers for different use cases.

## Hint 2
Overlay networks are for Docker Swarm across multiple hosts.

## Hint 3
Change driver: overlay to driver: bridge for single-host communication.

