## Objective
Fix the subclass so it properly implements the abstract method

## Hint 1
The Shape class requires two abstract methods. Are both implemented?

## Hint 2
Rectangle implements area() but is missing perimeter().

## Hint 3
Add perimeter() method: return 2 * (self.width + self.height).

