## Objective
Fix the function so it returns properly rounded averages

## Hint 1
The function says it returns floats but uses integer division (//).

## Hint 2
Integer division (//) truncates: 241//3 = 80 instead of 80.3.

## Hint 3
Change // to / and wrap with round(..., 1) for proper float results.

