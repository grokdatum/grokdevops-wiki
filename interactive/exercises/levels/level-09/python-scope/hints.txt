## Objective
Fix the variable scope so the script prints 'Counter: 3'

## Hint 1
Why can't the function modify the global variable?

## Hint 2
Using += on a variable inside a function makes Python treat it as local.

## Hint 3
Add 'global counter' as the first line inside the function.

