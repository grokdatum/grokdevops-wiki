## Objective
ARG declared before FROM must be re-declared after FROM to be usable

## Hint 1
ARG values declared before FROM have limited scope.

## Hint 2
An ARG before FROM is only available in the FROM line itself.

## Hint 3
Add ARG PYTHON_VERSION after the FROM line to make it available in the build stage.

