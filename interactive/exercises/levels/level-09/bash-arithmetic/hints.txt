## Objective
Fix the arithmetic calculation to use proper bash arithmetic syntax

## Hint 1
How does bash evaluate mathematical expressions?

## Hint 2
The expr command needs special handling, but bash has a built-in way.

## Hint 3
Use $(( expression )) for arithmetic: TOTAL=$(( BASE_SIZE * REPLICAS + OVERHEAD ))

