## Objective
Fix the playbook so the command module arguments are passed correctly using free_form

## Hint 1
Look at how the command arguments are being passed to the module.

## Hint 2
The command module uses 'cmd' parameter, not 'args' for the command string.

## Hint 3
Change 'args: df -h' to 'cmd: df -h'.

