## Objective
COPY instruction must reference the correct source file path

## Hint 1
The COPY instruction copies files from the build context into the image.

## Hint 2
Check if the source path in COPY matches where the file actually exists.

## Hint 3
The file app.py is in the root, not in src/. Change COPY src/app.py . to COPY app.py .

