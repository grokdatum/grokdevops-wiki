## Objective
Fix the script to use process substitution instead of a pipe to preserve variable scope

## Hint 1
Where do variables modified in a piped while loop actually live?

## Hint 2
Piping creates a subshell. Use process substitution < <(...) instead.

## Hint 3
Change 'cmd | while read' to 'while read ... done < <(cmd)'.

