## Objective
Environment variable must be properly passed to the service in docker-compose.yml

## Hint 1
Docker Compose has specific key names for configuration.

## Hint 2
The key for setting environment variables is 'environment:', not 'env:'.

## Hint 3
Change env: to environment: under the app service.

