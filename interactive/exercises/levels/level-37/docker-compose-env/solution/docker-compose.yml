services:
  app:
    image: python:3.11-slim
    command: python3 -c "import os; print(os.environ.get('DATABASE_URL', 'NOT SET'))"
    environment:
      DATABASE_URL: postgresql://db:5432/myapp
  db:
    image: postgres:15
    environment:
      POSTGRES_PASSWORD: secret
