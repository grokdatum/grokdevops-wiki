## Objective
Fix the caching so the function works with list input

## Hint 1
lru_cache requires all arguments to be hashable.

## Hint 2
Lists are not hashable, but tuples are.

## Hint 3
Convert the list to a tuple before passing: sum_items(tuple(numbers)).

