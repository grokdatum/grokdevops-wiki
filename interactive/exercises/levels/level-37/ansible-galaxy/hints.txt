## Objective
Fix the Galaxy requirements.yml to use the correct format for specifying roles

## Hint 1
ansible-galaxy fails to parse one of the collection entries.

## Hint 2
Each collection entry needs to use 'name:' key, not use the collection name as the key itself.

## Hint 3
Change '- community.docker:' to '- name: community.docker' (add 'name:' key).

