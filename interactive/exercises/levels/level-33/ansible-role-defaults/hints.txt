## Objective
Fix the role by moving variables that should be overridable from vars/ to defaults/

## Hint 1
Play-level variables aren't overriding the role variables.

## Hint 2
vars/main.yml has higher precedence than play vars; defaults/main.yml has lower precedence.

## Hint 3
Change all references from 'vars/main.yml' to 'defaults/main.yml' so variables can be overridden.

