## Objective
Bind mount must use correct relative path syntax with ./ prefix

## Hint 1
Docker Compose distinguishes between named volumes and bind mounts by syntax.

## Hint 2
A path without ./ or / prefix is treated as a named volume, not a bind mount.

## Hint 3
Change html:/usr/share/nginx/html to ./html:/usr/share/nginx/html.

