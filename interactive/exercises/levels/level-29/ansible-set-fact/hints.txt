## Objective
Fix the playbook to use set_fact correctly so the computed variable is available to subsequent tasks

## Hint 1
The module that sets dynamic variables is failing to execute.

## Hint 2
Check the module name - is it singular or plural?

## Hint 3
Change 'ansible.builtin.set_facts' to 'ansible.builtin.set_fact' (remove the 's').

