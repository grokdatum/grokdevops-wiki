services:
  proxy:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    networks:
      - appnet
  app:
    image: python:3.11-slim
    command: python3 -m http.server 5000
    networks:
      - appnet

networks:
  appnet:
