## Objective
Fix the descriptor so validated attributes work correctly

## Hint 1
The __set__ method stores the value, but where does it store it?

## Hint 2
setattr(obj, self.name, value) overwrites the descriptor itself with the value.

## Hint 3
Change self.name to self.storage_name in __set__: setattr(obj, self.storage_name, value).

