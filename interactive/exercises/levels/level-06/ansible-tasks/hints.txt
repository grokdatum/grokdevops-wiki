## Objective
Fix the playbook so every task has a descriptive 'name' field

## Hint 1
Ansible best practice requires every task to have a name - one is missing.

## Hint 2
Look at the copy task - it's missing the 'name' key.

## Hint 3
Add 'name: Copy application config file' (or similar) before the ansible.builtin.copy line.

