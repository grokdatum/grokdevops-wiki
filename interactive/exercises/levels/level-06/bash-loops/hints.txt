## Objective
Fix the while loop so it terminates correctly by incrementing the counter

## Hint 1
What keeps a while loop running? What makes it stop?

## Hint 2
The loop condition checks 'count' but count never changes.

## Hint 3
Add count=$((count + 1)) inside the loop body.

