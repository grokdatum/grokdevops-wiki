## Objective
EXPOSE port must match the port the application actually listens on

## Hint 1
Look at what port the Python server binds to in server.py.

## Hint 2
The EXPOSE port should match the port in HTTPServer(('0.0.0.0', PORT)).

## Hint 3
Change EXPOSE 3000 to EXPOSE 8080 to match the server.

