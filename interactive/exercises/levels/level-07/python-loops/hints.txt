## Objective
Fix the range so the script prints numbers 1 through 5, one per line

## Hint 1
How many numbers does range(1, 5) produce?

## Hint 2
range() excludes the stop value: range(1, 5) gives 1, 2, 3, 4.

## Hint 3
Change range(1, 5) to range(1, 6) to include 5.

