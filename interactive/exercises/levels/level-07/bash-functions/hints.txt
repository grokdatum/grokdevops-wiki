## Objective
Fix the script so the function is defined before it is called

## Hint 1
Bash reads scripts top to bottom. Can you use something before it exists?

## Hint 2
Functions must be defined before they are called in bash.

## Hint 3
Move the function definition above the line that calls it.

