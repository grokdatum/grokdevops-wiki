---
- name: System health check
  hosts: all
  become: true
  tasks:
    - name: Check uptime
      ansible.builtin.shell:
        cmd: uptime

    - name: List running processes with pipe
      ansible.builtin.command:
        cmd: ps aux | grep nginx

    - name: Check memory usage
      ansible.builtin.shell:
        cmd: free -m
