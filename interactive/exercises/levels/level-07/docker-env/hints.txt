## Objective
ENV instruction must use correct syntax to set environment variables

## Hint 1
Look at the ENV instructions. What separator is used between key and value?

## Hint 2
ENV uses = or space to separate key and value, not a colon.

## Hint 3
Change ENV APP_PORT:8080 to ENV APP_PORT=8080 (and same for APP_NAME).

