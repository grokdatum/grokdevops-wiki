## Objective
Fix the block/rescue structure so rescue is correctly nested within the block

## Hint 1
The block/rescue/always structure has an indentation problem.

## Hint 2
The rescue tasks have extra indentation - they should align with block tasks.

## Hint 3
Remove the extra indentation from rescue tasks so they align like: rescue: then - name: at same level as block tasks.

