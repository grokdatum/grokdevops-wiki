## Objective
Fix the sort command to sort numbers numerically instead of lexicographically

## Hint 1
Is 9 greater than 1000? It depends on how you sort.

## Hint 2
By default, sort uses lexicographic (dictionary) ordering.

## Hint 3
Add the -n flag: sort -n for numeric sorting.

