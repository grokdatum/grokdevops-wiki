## Objective
Fix the decorator so the original function metadata is preserved

## Hint 1
Check what __name__ and __doc__ return - they show the wrapper's metadata.

## Hint 2
functools.wraps copies the original function's metadata to the wrapper.

## Hint 3
Add @functools.wraps(func) before def wrapper(...).

