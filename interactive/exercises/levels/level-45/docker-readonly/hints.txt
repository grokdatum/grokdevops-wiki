## Objective
Container should run with read-only root filesystem for security

## Hint 1
A read-only filesystem prevents attackers from writing to the container.

## Hint 2
Use read_only: true and add tmpfs for directories that need writes.

## Hint 3
Add read_only: true and tmpfs: - /tmp to the service.

