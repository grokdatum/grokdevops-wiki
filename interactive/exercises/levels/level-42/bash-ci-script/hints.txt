## Objective
Fix the CI script to fail when tests fail by adding set -e

## Hint 1
What happens to the pipeline when a step fails?

## Hint 2
Without error handling, bash continues executing after failures.

## Hint 3
Add 'set -e' after the shebang to abort on any command failure.

