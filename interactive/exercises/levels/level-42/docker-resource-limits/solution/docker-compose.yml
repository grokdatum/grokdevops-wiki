services:
  app:
    image: python:3.11-slim
    command: python3 -c "print('Running with limits!')"
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: "0.50"
        reservations:
          memory: 128M
