## Objective
Container must have memory limits set to prevent resource exhaustion

## Hint 1
Without limits, a container can consume all host resources.

## Hint 2
Add a limits section alongside the existing reservations.

## Hint 3
Add limits: with memory: 256M and cpus: '0.50' under deploy.resources.

