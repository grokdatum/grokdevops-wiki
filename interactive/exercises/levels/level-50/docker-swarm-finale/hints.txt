## Objective
Fix multiple production issues: image tag, restart policy, resource limits, healthcheck, and logging

## Hint 1
There are multiple issues: image tags, restart policies, volumes, secrets, and healthchecks.

## Hint 2
Pin image versions, add restart policies, use named volumes, remove hardcoded secrets.

## Hint 3
Fix all: specific tags, restart: unless-stopped, named volumes, healthchecks, remove SECRET_KEY from compose.

