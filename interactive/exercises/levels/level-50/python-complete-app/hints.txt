## Objective
Fix all bugs in this complete application to produce correct output

## Hint 1
There are 3 bugs: check class variables, comparison operators, and division types.

## Hint 2
Bug 1: items={} is a class var (shared). Bug 2: < should be >=. Bug 3: // should be /.

## Hint 3
Move items={} to __init__, change < to >= in get_expensive_items, change // to / in apply_discount.

