## Objective
Fix the role structure so handlers are in the correct handlers/ directory

## Hint 1
The role's handler is never triggered - it can't find the handlers file.

## Hint 2
Ansible roles expect the handlers directory to be named 'handlers' (plural).

## Hint 3
Change 'handler/main.yml' to 'handlers/main.yml' everywhere in the file.

