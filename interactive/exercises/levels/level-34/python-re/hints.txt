## Objective
Fix the regex pattern so it correctly matches word boundaries

## Hint 1
The pattern uses \b but check how Python interprets backslashes in strings.

## Hint 2
Without a raw string, \b becomes a backspace character, not a word boundary.

## Hint 3
Add an r prefix: pattern = r"\bcat\b".

