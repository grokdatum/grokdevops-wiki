## Objective
tmpfs mount must use correct syntax in docker-compose.yml

## Hint 1
tmpfs mounts store data in memory, not on disk.

## Hint 2
tmpfs has its own key in docker-compose, not under volumes.

## Hint 3
Replace volumes: with tmpfs: and list the path /tmp/cache.

