## Objective
Fix the itertools usage to flatten the nested list

## Hint 1
chain() expects individual iterables as arguments, not a single nested list.

## Hint 2
chain.from_iterable() is designed to flatten a single iterable of iterables.

## Hint 3
Change chain(nested) to chain.from_iterable(nested).

