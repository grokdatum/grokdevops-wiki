## Objective
Fix the array declaration to use an associative array (declare -A)

## Hint 1
What type of array uses string keys instead of numeric indices?

## Hint 2
Regular arrays (declare -a) only support numeric indices.

## Hint 3
Change 'declare -a' to 'declare -A' for associative arrays.

