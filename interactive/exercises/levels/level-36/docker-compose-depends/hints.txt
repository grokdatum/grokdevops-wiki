## Objective
Use depends_on with condition: service_healthy and add a healthcheck to the dependency

## Hint 1
depends_on only waits for the container to start, not for it to be ready.

## Hint 2
Use condition: service_healthy and add a healthcheck to the dependency.

## Hint 3
Add healthcheck with pg_isready to db, and use depends_on: db: condition: service_healthy.

