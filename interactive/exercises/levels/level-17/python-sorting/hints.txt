## Objective
Fix the sorting so both the original and sorted lists are printed correctly

## Hint 1
list.sort() modifies the list in place and returns None.

## Hint 2
sorted() returns a new sorted list without modifying the original.

## Hint 3
Change 'scores.sort(reverse=True)' to 'sorted(scores, reverse=True)'.

