## Objective
Fix the playbook to use import_tasks for static inclusion since variables are used at parse time

## Hint 1
The playbook fails to load one of the task files - check the module names.

## Hint 2
One of the include/import module names is misspelled (singular vs plural).

## Hint 3
Change 'ansible.builtin.include_task' to 'ansible.builtin.include_tasks' (add the 's').

