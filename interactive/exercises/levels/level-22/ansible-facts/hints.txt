## Objective
Fix the playbook to use the correct Ansible facts key path for accessing OS information

## Hint 1
One of the facts references fails with a key error.

## Hint 2
The memory fact doesn't use a nested 'memory' key - it's a flat key name.

## Hint 3
Change 'ansible_facts.memory.total_mb' to 'ansible_facts.memtotal_mb'.

