services:
  web:
    image: nginx:alpine
    ports:
      - "8080:80"
    networks:
      - appnet
  api:
    image: python:3.11-slim
    command: python3 -m http.server 5000
    networks:
      - appnet

networks:
  appnet:
