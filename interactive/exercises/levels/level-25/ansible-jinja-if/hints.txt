## Objective
Fix the Jinja2 template in the playbook by adding the missing endif tag

## Hint 1
The template rendering fails with a Jinja2 syntax error.

## Hint 2
Every Jinja2 {% if %} block must be closed with {% endif %}.

## Hint 3
Add '{% endif %}' after the SSL configuration lines, before the location block.

