## Objective
Fix the curl command to silently fetch and correctly extract the HTTP status code

## Hint 1
Check what URL protocol is being used.

## Hint 2
The API endpoint uses https but the script is calling http.

## Hint 3
Change the URL from http:// to https://.

