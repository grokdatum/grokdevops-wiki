## Objective
Fix the property so temperature can be updated through a setter

## Hint 1
The code tries to set temperature but there is only a getter.

## Hint 2
A @property without a setter is read-only.

## Hint 3
Add a @temperature.setter method that sets self._temp = value.
