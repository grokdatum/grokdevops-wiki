## Objective
Dockerfile must create and switch to a non-root user for security

## Hint 1
Running containers as root is a security risk.

## Hint 2
Create a user with useradd and switch to it with USER.

## Hint 3
Add: RUN useradd --create-home appuser, then USER appuser before CMD.

