## Objective
Fix the pipeline so uniq correctly removes all duplicate lines

## Hint 1
Does uniq compare every line to every other line?

## Hint 2
uniq only removes adjacent duplicate lines. The data needs preprocessing.

## Hint 3
Pipe through sort before uniq: sort | uniq

