## Objective
Fix the role dependency definition to use the correct meta/main.yml syntax

## Hint 1
One of the role dependencies is not being resolved correctly.

## Hint 2
In meta/main.yml dependencies, use 'role:' key, not 'src:' (src is for requirements.yml).

## Hint 3
Change 'src: nginx' to 'role: nginx' in the second dependency.

