## Objective
Fix the debug output to go to stderr instead of stdout

## Hint 1
Where should diagnostic/debug output go - stdout or stderr?

## Hint 2
Debug messages on stdout get captured by $() and contaminate output.

## Hint 3
Add >&2 to the debug echo: echo "[DEBUG] $1" >&2

