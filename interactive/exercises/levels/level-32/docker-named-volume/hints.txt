## Objective
Replace anonymous volume with a named volume for data persistence

## Hint 1
Anonymous volumes are created with random names and hard to manage.

## Hint 2
Named volumes persist across container recreation and are easy to reference.

## Hint 3
Change /var/lib/postgresql/data to pgdata:/var/lib/postgresql/data and add a top-level volumes: section.

