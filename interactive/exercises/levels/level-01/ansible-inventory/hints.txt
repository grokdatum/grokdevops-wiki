## Objective
Fix the inventory file so host groups are properly defined with correct INI-style syntax

## Hint 1
Look at how each group name is defined - something is missing after one of them.

## Hint 2
In YAML inventory format, every group name must be followed by a colon character.

## Hint 3
Change "webservers" to "webservers:" - the colon is missing after the group name.

