## Objective
Pod must be Running

## Hint 1
Something's not right with this pod. Start by checking what's happening.

## Hint 2
The pod keeps crashing. Use `kubectl describe pod nginx-broken -n k8squest` to see the Events section. Look for error messages about what's failing.

## Hint 3
The error says "executable file not found" for "nginxzz". This command doesn't exist! The nginx container should just run normally without a custom command. Delete the pod and apply the solution.yaml which removes the bad command override.

