## Objective
Fix the dictionary so word counting works without KeyError

## Hint 1
The code tries to increment a key that might not exist yet.

## Hint 2
collections.defaultdict(int) starts missing keys at 0 automatically.

## Hint 3
Import defaultdict from collections and change {} to defaultdict(int).

