## Objective
Fix the grep command to use extended regex for matching IP addresses

## Hint 1
Basic and extended regex have different syntax for quantifiers.

## Hint 2
The + quantifier is an extended regex feature, not basic.

## Hint 3
Add the -E flag to grep to enable extended regular expressions.

