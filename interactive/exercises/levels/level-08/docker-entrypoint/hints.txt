## Objective
Use ENTRYPOINT for the executable and CMD for default arguments

## Hint 1
ENTRYPOINT defines the main executable; CMD provides default arguments.

## Hint 2
The order is wrong and the content is swapped. python3 should be in ENTRYPOINT.

## Hint 3
Use ENTRYPOINT ["python3", "app.py"] and CMD ["World"] for the default argument.

