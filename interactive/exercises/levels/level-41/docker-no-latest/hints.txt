## Objective
Use specific image tags instead of :latest for reproducible builds

## Hint 1
The :latest tag changes over time and can break production builds.

## Hint 2
Always pin to a specific version tag for reproducibility.

## Hint 3
Change FROM python:latest to FROM python:3.11-slim.

