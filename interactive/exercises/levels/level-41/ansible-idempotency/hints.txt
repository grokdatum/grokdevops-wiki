## Objective
Fix the shell task to be idempotent by adding the 'creates' parameter

## Hint 1
The shell task runs every time, even when the file already exists.

## Hint 2
Shell and command modules have a 'creates' parameter to skip execution if a file exists.

## Hint 3
Add 'creates: /opt/app-v2.0.tar.gz' to the shell task so it only runs once.

