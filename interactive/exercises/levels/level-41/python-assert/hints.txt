## Objective
Fix the assertion so it properly validates the condition

## Hint 1
Look at how assert is being called - is it a function call or a statement?

## Hint 2
assert(condition, message) creates a tuple (always truthy). assert is a statement, not a function.

## Hint 3
Remove the parentheses: 'assert age >= 0, "Age cannot be negative"'.

