## Objective
Fix the cut command to use the correct delimiter and field number

## Hint 1
What separates the fields in the config data?

## Hint 2
cut defaults to tab as delimiter; the data uses colons. Also check the field number.

## Hint 3
Use cut -d':' -f2 to extract the IP (second colon-separated field).

