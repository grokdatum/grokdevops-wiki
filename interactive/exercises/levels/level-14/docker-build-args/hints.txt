## Objective
ARG must have a default value so the build works without --build-arg

## Hint 1
ARG without a default value will be empty if --build-arg is not provided.

## Hint 2
Give the ARG a default value using the = syntax.

## Hint 3
Change ARG APP_VERSION to ARG APP_VERSION=1.0.0

