## Objective
Fix the set operation so the script prints the common skills

## Hint 1
The comment says 'BOTH sets' but check what operation is actually used.

## Hint 2
union() combines all elements; intersection() finds common elements.

## Hint 3
Change .union(backend) to .intersection(backend).

