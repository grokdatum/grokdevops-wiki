## Objective
Fix the redirect order so stderr and stdout are both captured

## Hint 1
The order of redirections matters. Which gets applied first?

## Hint 2
2>&1 redirects stderr to wherever stdout currently points.

## Hint 3
Put the file redirect first: command > file 2>&1 (not 2>&1 > file).

