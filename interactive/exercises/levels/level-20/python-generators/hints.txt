## Objective
Fix the code so both sum and average calculations work

## Hint 1
A generator can only be iterated once - after that it is exhausted.

## Hint 2
sum(nums) consumes the generator, so list(nums) returns an empty list.

## Hint 3
Convert the generator to a list first: nums = list(squares(5)).

