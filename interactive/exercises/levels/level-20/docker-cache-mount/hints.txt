## Objective
RUN --mount=type=cache must use correct target path for pip cache

## Hint 1
The --mount cache target must match where the tool stores its cache.

## Hint 2
pip's default cache is at /root/.cache/pip. The mount target and pip must agree.

## Hint 3
Change target to /root/.cache/pip and remove the --cache-dir flag from pip install.

