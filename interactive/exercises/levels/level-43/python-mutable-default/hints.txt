## Objective
Fix the function so each call gets a fresh list

## Hint 1
Default mutable arguments in Python are created once and shared across calls.

## Hint 2
The same list object is reused every time the function is called without items.

## Hint 3
Use None as default and create a new list inside: if items is None: items = [].

