## Objective
Container must have a restart policy to recover from failures

## Hint 1
Without a restart policy, a crashed container stays stopped.

## Hint 2
Docker supports restart policies: no, always, unless-stopped, on-failure.

## Hint 3
Add restart: unless-stopped to the service.

