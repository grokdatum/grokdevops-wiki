## Objective
Two services on host network cannot use the same port - fix the conflict

## Hint 1
Host network mode shares the host's network stack directly.

## Hint 2
Two containers on host network cannot bind to the same port.

## Hint 3
Change web2's NGINX_PORT to a different port like 8081.

