## Objective
Fix the useradd command simulation to include the home directory flag

## Hint 1
What flag does useradd need to create a home directory?

## Hint 2
Without -m, useradd will not create /home/username.

## Hint 3
Add -m flag: useradd -m $USERNAME

