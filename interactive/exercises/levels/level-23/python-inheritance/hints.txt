## Objective
Fix the multiple inheritance so all attributes are initialized

## Hint 1
With multiple inheritance, calling Parent.__init__ directly can skip some parents.

## Hint 2
Use super().__init__() in all classes to follow the MRO chain.

## Hint 3
Replace all explicit Parent.__init__(self) calls with super().__init__().

