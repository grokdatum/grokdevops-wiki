## Objective
Fix the script to fail fast on errors using set -e

## Hint 1
What bash option makes a script stop on the first error?

## Hint 2
Without set -e, bash continues even when commands fail.

## Hint 3
Add 'set -e' near the top of the script (after the shebang).

