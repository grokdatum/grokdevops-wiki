---
- name: Apply nginx role
  hosts: webservers
  become: true
  roles:
    - role: nginx

  # Role structure reference:
  # roles/nginx/
  #   tasks/main.yml    <-- FIXED: correct directory name
  #   handlers/main.yml
  #   templates/nginx.conf.j2
  #   defaults/main.yml

# roles/nginx/tasks/main.yml content:
# Role tasks are now in the correct directory
- name: Simulate role tasks file
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    role_dir: "roles/nginx/tasks/main.yml"
  tasks:
    - name: Show role directory structure
      ansible.builtin.debug:
        msg: "Role tasks are in: {{ role_dir }}"
