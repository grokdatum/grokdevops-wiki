## Objective
Fix the path construction using pathlib properly

## Hint 1
Path objects don't support string concatenation with +.

## Hint 2
pathlib uses the / operator to join path components.

## Hint 3
Change '+' to '/' for path joining: base / 'config' / 'settings.json'.

