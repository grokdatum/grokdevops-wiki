## Objective
Container must find and run the application file by setting the correct WORKDIR

## Hint 1
The CMD references /app/index.js but where is the file copied to?

## Hint 2
Without WORKDIR, COPY places files in the root directory, not /app/.

## Hint 3
Add WORKDIR /app before the COPY instruction.

