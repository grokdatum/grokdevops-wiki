## Objective
Fix the YAML indentation so the playbook parses correctly

## Hint 1
The YAML parser is failing - look for inconsistent indentation.

## Hint 2
Check the indentation under the apt module - one key has extra spaces.

## Hint 3
The 'state: present' line has an extra space - align it with 'name: nginx' above it.

