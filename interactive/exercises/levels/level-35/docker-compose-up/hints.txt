## Objective
docker-compose.yml must have correct YAML indentation and syntax

## Hint 1
YAML is whitespace-sensitive. Indentation determines structure.

## Hint 2
ports must be a child of the web service, command a child of api.

## Hint 3
Indent ports: under web: and command: under api: with 4 spaces.

