## Objective
Fix the xargs parallel execution to use correct syntax for concurrent jobs

## Hint 1
How does xargs pass arguments and control parallelism?

## Hint 2
-P controls parallel jobs, -I{} sets the placeholder for arguments.

## Hint 3
Use xargs -I{} -P3 bash -c 'process_server "$@"' _ {} for proper parallel execution.

