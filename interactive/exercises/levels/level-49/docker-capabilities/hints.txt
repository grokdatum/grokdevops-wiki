## Objective
Container must drop all capabilities and add only what's needed

## Hint 1
privileged: true gives the container full access to the host.

## Hint 2
Use cap_drop: ALL and only add specific capabilities needed.

## Hint 3
Remove privileged: true, add cap_drop: ALL, cap_add only what's needed.

