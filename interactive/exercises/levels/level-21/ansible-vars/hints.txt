## Objective
Fix the playbook so the variable is defined at the correct scope level for all tasks to access it

## Hint 1
The second and third tasks fail because they can't find the variable.

## Hint 2
The variable is defined at task level (under vars: in the first task) - it's not accessible to other tasks.

## Hint 3
Move 'vars: app_name: mywebapp' to play level (same indent as 'hosts:' and 'tasks:').

