## Objective
EXPOSE port must match the port the application listens on inside the container

## Hint 1
Look at server.py to see what port the application binds to.

## Hint 2
EXPOSE should document the actual port the application uses.

## Hint 3
Change EXPOSE 80 to EXPOSE 5000 to match the server.

