## Objective
Fix the ps output parsing to extract the correct field (PID)

## Hint 1
Look at the columns in ps output. Which column has the PID?

## Hint 2
ps output columns: PID($1) TTY($2) TIME($3) CMD($4).

## Hint 3
Change awk '{print $3}' to awk '{print $1}' to get PID.

