## Objective
Fix xargs to properly handle filenames containing spaces

## Hint 1
What happens when xargs encounters a space in a filename?

## Hint 2
Use null-byte delimiters instead of whitespace for safe filename handling.

## Hint 3
Use find -print0 | xargs -0 to handle filenames with spaces.

