## Objective
Fix the serial keyword to use an integer value instead of a string

## Hint 1
The playbook fails when trying to determine batch size for rolling updates.

## Hint 2
The serial keyword expects a numeric value, not a word.

## Hint 3
Change 'serial: "two"' to 'serial: 2'.

