## Objective
Fix the unpacking so the script correctly parses the record

## Hint 1
Count how many values are in the tuple vs how many variables to unpack.

## Hint 2
The tuple has 5 elements but only 3 variables are given.

## Hint 3
Use *_ to capture the extra values: name, age, job, *_ = record.

