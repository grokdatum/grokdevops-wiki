## Objective
Fix the vault variable reference to use the correct variable name format

## Hint 1
The database password variable is undefined when the playbook runs.

## Hint 2
Vault-encrypted variables are referenced by their original variable name, not with a 'vault_' prefix.

## Hint 3
Change '{{ vault_db_password }}' to '{{ db_password }}' - the variable name doesn't change when encrypted.

