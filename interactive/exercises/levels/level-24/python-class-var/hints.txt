## Objective
Fix the class so each student has their own independent grades list

## Hint 1
Check where the grades list is defined - at class level or instance level.

## Hint 2
A mutable class variable is shared by all instances.

## Hint 3
Move 'self.grades = []' into __init__ so each instance gets its own list.

