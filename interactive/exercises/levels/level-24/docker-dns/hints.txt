## Objective
Containers must use service names (not localhost) to communicate in Docker networks

## Hint 1
Docker provides built-in DNS for containers on custom networks.

## Hint 2
Containers should use service names, not localhost, to reach other containers.

## Hint 3
Replace localhost with redis in the connection URL in app.py.

