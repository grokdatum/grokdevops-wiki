## Objective
Fix the context manager so exceptions are properly suppressed

## Hint 1
__exit__ must return a truthy value to suppress the exception.

## Hint 2
Returning False (or None) from __exit__ lets the exception propagate.

## Hint 3
Change 'return False' to 'return True' when the exception should be suppressed.

