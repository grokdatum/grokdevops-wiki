## Objective
Fix the file descriptor redirect order to capture stderr separately from stdout

## Hint 1
In what order are redirections applied, and what does 2>&1 actually do?

## Hint 2
The 2>&1 makes stderr point to wherever stdout is at that moment.

## Hint 3
Simply use 1>stdout.log 2>stderr.log without any 2>&1.

