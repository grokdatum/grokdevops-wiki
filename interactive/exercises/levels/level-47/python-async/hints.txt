## Objective
Fix the async code so the coroutine is properly awaited

## Hint 1
Calling an async function without await returns a coroutine object, not the result.

## Hint 2
You need to use 'await' when calling async functions to get their return value.

## Hint 3
Change 'fetch_data(...)' to 'await fetch_data(...)' in both calls.

