## Objective
Fix the vault-encrypted variable format so it can be properly decrypted

## Hint 1
One of the vault-encrypted variables fails to decrypt properly.

## Hint 2
Vault inline encrypted strings need the pipe (|) for literal block scalar notation.

## Hint 3
Change '!vault' to '!vault |' for the api_key variable (add the pipe character).

