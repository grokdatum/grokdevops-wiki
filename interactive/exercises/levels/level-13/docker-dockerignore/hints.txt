## Objective
Create a .dockerignore to exclude node_modules and other unnecessary files

## Hint 1
Docker sends the entire build context to the daemon. Large directories slow this down.

## Hint 2
A .dockerignore file works like .gitignore - it excludes files from the build context.

## Hint 3
Create a .dockerignore file with node_modules on its own line.

