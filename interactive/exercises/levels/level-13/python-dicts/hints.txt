## Objective
Fix the dictionary key so the script prints 'Email: alice@example.com'

## Hint 1
Compare the key used in the lookup with the keys defined in the dictionary.

## Hint 2
The dictionary uses 'email' but the code accesses 'e-mail' (with a hyphen).

## Hint 3
Change user["e-mail"] to user["email"].

