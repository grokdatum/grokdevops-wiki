## Objective
Fix the when condition to use correct Jinja2 comparison syntax

## Hint 1
One of the when conditions has a syntax error in the comparison.

## Hint 2
Python/Jinja2 uses '==' for comparison, not '=' which is assignment.

## Hint 3
Change 'ansible_os_family = "Debian"' to 'ansible_os_family == "Debian"' (double equals).

