## Objective
Fix the grep command to count matching lines instead of listing filenames

## Hint 1
What grep flag gives you a number instead of text?

## Hint 2
The -l flag lists filenames; you need a flag that counts.

## Hint 3
Use grep -c instead of grep -l to count matching lines.

