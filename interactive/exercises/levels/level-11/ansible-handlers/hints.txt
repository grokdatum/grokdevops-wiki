## Objective
Fix the playbook so the notify directive matches the handler name exactly

## Hint 1
The handler is never triggered even when the config file changes.

## Hint 2
The 'notify' value must exactly match the handler's 'name' field - compare them carefully.

## Hint 3
Change 'notify: restart nginx' to 'notify: Restart nginx service' to match the handler name.

