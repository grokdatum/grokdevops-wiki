## Objective
Copy dependency file first, install dependencies, then copy source for better cache utilization

## Hint 1
Docker caches each layer. If any file changes, all subsequent layers rebuild.

## Hint 2
Dependencies change less often than source code. Copy and install them separately.

## Hint 3
Split: COPY requirements.txt . -> RUN pip install -> COPY . .

