## Objective
Logging must be properly configured with max-size and max-file options

## Hint 1
Without log rotation, container logs can fill up the entire disk.

## Hint 2
Add options to the json-file driver to limit log file size and count.

## Hint 3
Add options: with max-size: "10m" and max-file: "3" under logging.

