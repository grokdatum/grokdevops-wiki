## Objective
Fix the closure so each function captures its own value

## Hint 1
All three lambdas produce the same result. Why?

## Hint 2
Closures capture variables by reference, not by value. All see the final i=3.

## Hint 3
Use a default argument to capture: lambda x, i=i: x * i.

