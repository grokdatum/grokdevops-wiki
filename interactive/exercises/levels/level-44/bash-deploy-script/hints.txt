## Objective
Fix the deploy script to check each step's exit status before continuing

## Hint 1
What happens between the migration step and the app start step?

## Hint 2
The migration can fail but the script continues to start the app anyway.

## Hint 3
Add a check: if migration failed (non-zero), abort and return 1.

