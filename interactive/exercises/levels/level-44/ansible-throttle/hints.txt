## Objective
Fix the playbook to correctly use throttle to limit concurrent task execution

## Hint 1
The throttle setting is being ignored - migrations run on all hosts simultaneously.

## Hint 2
The throttle keyword expects a numeric value, not a string.

## Hint 3
Change 'throttle: "one"' to 'throttle: 1'.

