## Objective
Fix the indentation so the script prints the correct greeting

## Hint 1
Check the indentation of each line inside the if/else block.

## Hint 2
The print(message) and the message assignment should be at the same indent level.

## Hint 3
Both lines inside the if block should have exactly 8 spaces (one indent level inside the function + one inside the if).

