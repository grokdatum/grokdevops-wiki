## Objective
CMD must use proper JSON exec form with correct syntax

## Hint 1
The CMD instruction has two forms: shell form and exec (JSON) form.

## Hint 2
The exec form requires a proper JSON array with double-quoted strings.

## Hint 3
Change CMD [python3, app.py] to CMD ["python3", "app.py"]

