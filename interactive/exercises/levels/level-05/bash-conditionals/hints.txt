## Objective
Fix the comparison operator to correctly compare numeric values

## Hint 1
How does bash compare numbers versus strings?

## Hint 2
String operators: =, != | Numeric operators: -eq, -ne, -gt, -ge, -lt, -le

## Hint 3
Use -ge instead of = and swap the if/else branches (or rewrite the condition).

