## Objective
Fix the type conversion so the script prints 'Double: 20'

## Hint 1
What happens when you multiply a string by 2 in Python?

## Hint 2
Multiplying a string repeats it: '10' * 2 = '1010', not 20.

## Hint 3
Convert the string to an integer first: int(user_input) * 2.

