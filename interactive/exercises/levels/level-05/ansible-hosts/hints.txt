## Objective
Fix the playbook to target the correct host group that matches the inventory

## Hint 1
The playbook cannot find any hosts to run against - check the hosts line.

## Hint 2
The host group name in the playbook doesn't match what's in the inventory.

## Hint 3
Change 'hosts: web_servers' to 'hosts: webservers' (no underscore).

