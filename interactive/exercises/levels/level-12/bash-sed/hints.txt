## Objective
Fix the sed command to replace all occurrences on each line, not just the first

## Hint 1
How many substitutions does sed make by default per line?

## Hint 2
By default, sed only replaces the first match on each line.

## Hint 3
Add the 'g' flag: sed 's/\t/ /g' to replace all occurrences.

