## Objective
Fix the metaclass so it correctly registers classes with a greeting method

## Hint 1
The code expects Plugin to be in the registry but it says 'Registry has Plugin: False'.

## Hint 2
The 'if bases:' check skips registering Plugin because it has no bases (it IS the base).

## Hint 3
Remove the 'if bases:' check so all classes using the metaclass are registered.

