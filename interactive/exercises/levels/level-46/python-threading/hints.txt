## Objective
Fix the shared counter to be thread-safe using a lock

## Hint 1
Multiple threads modify counter without any synchronization.

## Hint 2
Use threading.Lock() to prevent race conditions on shared data.

## Hint 3
Create lock = threading.Lock() and wrap the read-modify-write with 'with lock:'.

