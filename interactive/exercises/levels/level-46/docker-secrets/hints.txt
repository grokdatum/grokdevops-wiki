## Objective
Secrets must not be hardcoded in Dockerfile - use environment variables or Docker secrets

## Hint 1
Secrets in ENV instructions get baked into the image layers.

## Hint 2
Anyone with access to the image can extract these secrets.

## Hint 3
Remove the ENV lines with secrets. Pass them at runtime via docker-compose environment:.

