## Objective
Fix the dunder method so print() shows the user-friendly format

## Hint 1
print() and f-strings use __str__, falling back to __repr__ if no __str__ exists.

## Hint 2
The output shows 'Point(3, 4)' but we want just '(3, 4)'.

## Hint 3
Add a __str__ method that returns the simpler format '({self.x}, {self.y})'.

