## Objective
Fix the inverted service status check logic

## Hint 1
What does an exit code of 0 mean in Unix?

## Hint 2
Exit code 0 = success = service is active. The check is backwards.

## Hint 3
Change '-ne 0' to '-eq 0' (or swap the if/else messages).

