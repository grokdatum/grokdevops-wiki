## Objective
Port mapping in docker-compose must use correct host:container order

## Hint 1
Docker port mapping format is -p HOST_PORT:CONTAINER_PORT.

## Hint 2
nginx listens on port 80 inside the container.

## Hint 3
Change 80:8080 to 8080:80 (host 8080 maps to container 80).

