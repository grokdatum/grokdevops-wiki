## Objective
Build context and Dockerfile path must be correct in docker-compose.yml

## Hint 1
Build context defines the root directory sent to the Docker daemon.

## Hint 2
The Dockerfile and app.py are in the current directory, not in ./app/.

## Hint 3
Change context: ./app to context: . since files are in the root.

