## Objective
Fix the argparse configuration so the greeting repeats correctly

## Hint 1
What type does argparse assign to --times by default?

## Hint 2
Without type=int, argparse treats command line values as strings. range('3') fails.

## Hint 3
Add type=int to the --times argument definition.

