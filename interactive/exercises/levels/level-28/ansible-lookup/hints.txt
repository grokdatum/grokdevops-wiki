## Objective
Fix the lookup plugin syntax to correctly read a file

## Hint 1
One of the lookup calls fails - the plugin name is wrong.

## Hint 2
The Ansible file lookup plugin is singular, not plural.

## Hint 3
Change lookup('files', ...) to lookup('file', ...) in the ssl_certificate variable.

