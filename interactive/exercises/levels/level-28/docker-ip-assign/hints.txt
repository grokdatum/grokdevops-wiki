## Objective
Static IP must be within the defined network subnet range

## Hint 1
Static IPs must fall within the network's defined subnet.

## Hint 2
The subnet is 172.20.0.0/16 but the IP is 10.0.0.100.

## Hint 3
Change ipv4_address to 172.20.0.100 to match the 172.20.0.0/16 subnet.

