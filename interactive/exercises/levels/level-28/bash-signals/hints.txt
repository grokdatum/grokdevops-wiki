## Objective
Fix the trap to catch the correct signal for graceful shutdown

## Hint 1
What signal does Docker/Kubernetes send for graceful shutdown?

## Hint 2
SIGTERM (not SIGUSR1) is the standard graceful shutdown signal.

## Hint 3
Change 'trap cleanup USR1' to 'trap cleanup TERM'.

