## Objective
Fix the __slots__ definition so the email attribute works

## Hint 1
__slots__ restricts which attributes an instance can have.

## Hint 2
The class tries to set self.email but 'email' is not in __slots__.

## Hint 3
Add 'email' to __slots__: __slots__ = ('name', 'age', 'email').

