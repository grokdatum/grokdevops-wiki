## Objective
Fix subprocess.run to capture and display command output

## Hint 1
result.stdout is None because the output was not captured.

## Hint 2
subprocess.run needs capture_output=True to capture stdout and stderr.

## Hint 3
Add capture_output=True to the subprocess.run call.

