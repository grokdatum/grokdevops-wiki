## Objective
Fix the inventory plugin configuration to use the correct plugin name

## Hint 1
The dynamic inventory plugin fails to load - it can't find the plugin.

## Hint 2
Modern Ansible requires fully qualified collection names (FQCN) for plugins.

## Hint 3
Change 'plugin: aws_ec2' to 'plugin: amazon.aws.aws_ec2'.

